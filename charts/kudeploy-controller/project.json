{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "kudeploy-controller",
  "root": "charts/kudeploy-controller",
  "tags": ["type:helm-chart"],
  "implicitDependencies": ["controller"],
  "targets": {
    "dependency-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "helm dependency build charts/kudeploy-controller"
      },
      "cache": false
    }
  }
}
