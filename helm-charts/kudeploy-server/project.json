{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "server-helm-chart",
  "root": "helm-charts/kudeploy-server",
  "projectType": "application",
  "tags": ["type:helm-chart"],
  "metadata": {
    "description": "Kudeploy API server and web application."
  },
  "release": {
    "version": {
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk",
      "versionActions": "nx-helm/release"
    }
  },
  "implicitDependencies": ["server"],
  "targets": {
    "dependency-build": {
      "executor": "nx-helm:dependency-build",
      "options": {
        "chartRoot": "helm-charts/kudeploy-server",
        "distDir": "dist"
      },
      "cache": false,
      "inputs": ["default", "{workspaceRoot}/pnpm-lock.yaml"],
      "dependsOn": [
        { "dependencies": true, "target": "package", "params": "ignore" }
      ]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "helm lint helm-charts/kudeploy-server"
      },
      "cache": true,
      "inputs": ["default", "^default", "{workspaceRoot}/pnpm-lock.yaml"],
      "dependsOn": ["dependency-build"]
    },
    "package": {
      "executor": "nx-helm:package",
      "options": {
        "chartRoot": "helm-charts/kudeploy-server",
        "distDir": "dist",
        "repository": "oci://ghcr.io/kudeploy/helm-charts"
      },
      "cache": true,
      "inputs": ["default", "^default", "{workspaceRoot}/pnpm-lock.yaml"],
      "outputs": ["{workspaceRoot}/dist/kudeploy-server-*.tgz"],
      "dependsOn": [
        { "dependencies": true, "target": "package", "params": "ignore" },
        "dependency-build"
      ]
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "helm push dist/kudeploy-server-*.tgz oci://ghcr.io/kudeploy/helm-charts"
      },
      "cache": false,
      "dependsOn": ["package"]
    },
    "nx-release-publish": {
      "executor": "nx-helm:release-publish",
      "options": {
        "chartName": "kudeploy-server",
        "chartRoot": "helm-charts/kudeploy-server",
        "distDir": "dist",
        "repository": "oci://ghcr.io/kudeploy/helm-charts"
      },
      "cache": false,
      "dependsOn": ["lint", "package"]
    }
  }
}
