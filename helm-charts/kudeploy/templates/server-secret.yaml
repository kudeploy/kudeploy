{{- if and .Values.server.enabled (not .Values.server.crypt.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kudeploy.server.crypt.secretName" . }}
  namespace: {{ include "kudeploy.server.namespace" . }}
  labels:
    {{- include "kudeploy.server.labels" . | nindent 4 }}
  {{- with .Values.server.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{ .Values.server.crypt.secretKey }}: {{ include "kudeploy.server.crypt.secretValue" . | b64enc | quote }}
{{- end }}
